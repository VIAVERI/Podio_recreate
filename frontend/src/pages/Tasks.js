// src/pages/Tasks.js
import React from 'react';
import { Typography } from '@mui/material';

const Tasks = () => {
    return (
        <div>
            <Typography variant="h4">Tasks</Typography>
            <Typography>Manage your tasks here.</Typography>
            {/* Add task management components here */}
        </div>
    );
};

export default Tasks;
